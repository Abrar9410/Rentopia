import { Metadata } from "next";


export const metadata: Metadata = {
    title: "Contact | Rentopia",
    description: "This page contains the contact information of Rentopia. Users can complain, seek help or give feedback by using the contact form."
};

const ContactPage = () => {
    return (
        <div>
            CONTACT PAGE
        </div>
    );
};

export default ContactPage;