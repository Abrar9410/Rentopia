import { Metadata } from "next";


export const metadata: Metadata = {
    title: "My Profile | Rentopia",
    description: "User Profile page containing User information."
};


const ProfilePage = () => {
    return (
        <div>
            MY PROFILE
        </div>
    );
};

export default ProfilePage;