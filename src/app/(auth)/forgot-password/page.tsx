import { Metadata } from "next";


export const metadata: Metadata = {
    title: "Forgot Password | Rentopia",
    description: "User has to provide his/her account email to get a Reset-Password link through email.",
};


const ForgotPasswordPage = () => {
    return (
        <div>
            ForgotPasswordForm
        </div>
    );
};

export default ForgotPasswordPage;